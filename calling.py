class CallManager:
    def make_call(self, contact_name):
        print(f"Calling {contact_name}...")
        # Simulated call logic
        print(f"Connected to {contact_name}. Elvira is now speaking on your behalf.")